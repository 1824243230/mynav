cd ~/TGH_ws

git status

git add .



git commit -m "其他路径导航改好了"
git push

编译第三方包：
  mkdir build && cd build && cmake ..
  make
  sudo make install
编译完成后
source /opt/ros/noetic/setup.bash
source devel/setup.bash
运行仿真：
  roslaunch plan_manage kino_replan.launch B_Spline_LocalPlanner:=true sim_pose:=true


g
修改地图颜色：
现有 RViz 显示项位于 [traj_TGH.rviz (line 798)](/home/hyh/mycar_ws/src/TGH-Planner/TGH_Planner/plan_manage/config/traj_TGH.rviz:798)，名称为：unknown2D

修改地图图片：
/home/hyh/mycar_ws/src/TGH-Planner/Utils/topo_prm_planner/maps 这个文件夹中


cd /home/hyh/mycar_ws
source /opt/ros/noetic/setup.bash
catkin_make -DCMAKE_BUILD_TYPE=Release
source devel/setup.bash
roslaunch topo_prm_planner topo_prm_planner.launch


  roslaunch plan_manage kino_replan.launch B_Spline_LocalPlanner:=true sim_pose:=true
  探索过后的地图底色是255，如何设置。
  同时
  在png这种地图导航探索该如何实现


保存SDF二维地图：

  # 必须在每个新终端加载当前工作空间，不要加载旧的 TGH_ws
  source /opt/ros/noetic/setup.bash
  source /home/hyh/mycar_ws/devel/setup.bash

  # 确认 ROS 能找到该包和 launch 文件
  rospack find save_map_point
  roslaunch --files save_map_point map_save.launch

  # 先启动建图/规划仿真，再在另一个已 source 的终端运行
  roslaunch save_map_point map_save.launch

  # 默认保存位置
  # /home/hyh/mycar_ws/src/TGH-Planner/Utils/save_map_point/map_point/map_points.txt





现在进行最终收尾。

不要继续增加新的核心算法。

本阶段只进行：

1. B-Spline warm start
2. 必要的前端连续性处理
3. 日志
4. 参数整理
5. 编译
6. 测试准备

--------------------------------
一、B-Spline Warm Start
--------------------------------

检查当前 local planner 初始化方式。

如果每次 replanning 都完全使用新的 Hybrid A* 路径重新初始化 B-Spline，

尝试：

优先使用当前 active trajectory 剩余部分
作为新优化的 warm start。

从车辆当前实际执行位置以后
裁剪 active trajectory，

重新采样后用于新的 B-Spline initialization。

如果代码结构不适合完整 warm start，
不要强行大重构。

--------------------------------
二、前端控制点连续性
--------------------------------

如果容易实现，

固定 B-Spline 最前面 2~3 个控制点，

或者使用现有边界条件，

保证新轨迹与当前执行状态：

position
velocity
acceleration

尽量连续。

但是：

不要新增 temporal consistency objective。

不要修改原来的：

smoothness
obstacle
feasibility
AVC

主要目标函数。

--------------------------------
三、整理 ECTA 参数
--------------------------------

确认存在并有默认值：

ecta/enabled

ecta/use_fixed_scale_score

ecta/length_ref
ecta/risk_ref
ecta/prs_ref

ecta/lambda_switch
ecta/switch_margin
ecta/dubins_ref

ecta/min_topology_eval_distance

ecta/protect_active_topology

ecta/atomic_commit

ecta/single_flight_replan

根据实际项目命名风格调整。

不要保留无用参数。

--------------------------------
四、完善日志
--------------------------------

增加可用于实验分析的日志：

timestamp

active topology/path

keep path
challenger

J_keep
J_challenger

route_gain

D_keep
D_challenger

switch_cost

switching_surplus

decision

replan_reason

planning_success

commit_success

odom x/y/yaw

execution progress

active trajectory version

日志使用：

ROS_DEBUG
ROS_INFO_THROTTLE
或 verbose 参数

避免高频刷屏。

--------------------------------
五、增加统计量
--------------------------------

尽可能增加：

topology_switch_count

topology_reversal_count

其中 reversal 定义为类似：

A -> B -> A

replan_count

failed_plan_count

trajectory_commit_count

failed_plan_without_active_trajectory

最后一个指标目标必须为：

0

--------------------------------
六、编译
--------------------------------

执行完整 catkin 编译。

如果出现：

C++
link
header
ROS parameter
message
launch

错误，

继续修复直到编译成功。

不要通过注释掉功能绕过编译问题。

--------------------------------
七、最终代码检查
--------------------------------

检查：

1.
所有新增 bool 初始化

2.
所有新增 double 初始化

3.
没有 magic number

4.
没有 selected / proposal / candidate 混用

5.
candidate 成功前不会修改 active topology

6.
candidate 成功前不会停止 active trajectory

7.
planner 不会重入

8.
selected active path 不会被普通裁剪删除

9.
当前 topology invalid 可以绕过稳定机制

10.
Fixed-scale score 默认启用

--------------------------------
八、最终输出总结
--------------------------------

最后给我一份修改报告：

A. 修改文件列表

B. 每个文件修改了什么

C. ECTA 数学公式：

G =
(J_keep - J_challenger)
/
(J_keep + epsilon)

C_switch =
max(0, D_challenger - D_keep)
/
D_ref

S =
G - lambda_switch * C_switch

正常：

S > switch_margin

才产生 topology switch proposal

当前 topology invalid：

立即允许 proposal

但只有 candidate trajectory 成功：

才 Atomic Commit

D. 给出新的完整调用链

E. 给出推荐仿真测试步骤

F. 列出你认为仍然存在的潜在风险或 TODO

完成后不要自行继续引入其他算法。